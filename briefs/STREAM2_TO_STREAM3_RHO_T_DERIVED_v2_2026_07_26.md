# Stream 2 → Stream 3: ρ = 19, T = 3 — **derived**. Criterion 1 is now re-scorable.

**Date:** 2026-07-26 · **Ticket:** `ESCALATIONS.md` **E-011** · **Authority:** Xavier (T0), 2026-07-26
**Action required:** re-score Gate E criterion 1 against ρ=19/T=3. **No batch re-run needed.**

> ⚠️ **Read this before acting.** An earlier brief with a near-identical name —
> `STREAM2_TO_STREAM3_RHO_T_DERIVED_2026_07_26.md` — was **fabricated and withdrawn**
> (`ESCALATIONS.md` E-010). It was reverted and never pushed. **This one is different, and
> here is how you check that rather than taking my word for it:**
>
> | | withdrawn brief (E-010) | this brief (E-011) |
> |---|---|---|
> | where ρ came from | `picard_rank = 19` hardcoded in the checker | computed as `b₂ − rank_V`, reading `rank_V` from a certificate at runtime |
> | Mordell–Weil rank | back-solved to make `19 = 2+2+15` close | not used at all; not claimed |
> | discriminant | `−3` re-introduced (retracted in E-007) | stays `null` |
> | sources | a paywalled paper nobody had read | two sources fetched, hash-pinned, and read |
> | falsifiable? | no | yes — degrade step A and the checker refuses to emit |
>
> Verify it yourself: `python3 checkers/check_C2_transcendental_rank.py` (dry run, writes
> nothing). Then read `checkers/check_L3_irreducible_minimal.py`. **Read the source, not the
> certificate** — that is the E-010 lesson.

## What to do

1. **Score criterion 1** against **ρ = 19, T = 3**. Your retained `lattice_chi2`,
   `picard_estimate`, `transcendental_estimate` per sector are exactly the inputs — this is
   why T0 decision D1 said retain rather than discard.
2. **Do not re-run the batch.** Nothing about the observable changed; only the prior it is
   compared against.
3. **Do not compare against ρ=4/T=18** under any circumstance. Permanently retracted (E-007),
   and arithmetically impossible: an order-3 operator governs a rank-3 system, so T=18 would
   need an order-18 operator. The retracted values were essentially *inverted*.
4. **Carry the caveats into the verdict text** (§ below). A verdict that states ρ=19 without
   them is overclaiming.

## The caveats — these are not boilerplate

- **VERY GENERAL member.** ρ = 19 is the generic value. It **jumps to 20 on a countable dense
  subset** of the base (Noether–Lefschetz locus, singular K3s). If your sectors are being
  matched against individual fibres rather than the generic one, this matters to how you
  score, and you should say so in the verdict.
- **Tier [B], not [A].** The final step is a citation to published work, not something derived
  in this repo. Score it as strong evidence, not as kernel-verified.
- **`discriminant` is `null`.** If any part of your pipeline still consumes a discriminant,
  it is consuming a retracted number — check and report.
- Applies to **s7 and s10 only**. Nothing about s18.

## Where ρ = 19 comes from

1. **[A, computed]** L₃ is irreducible, hence the minimal-order Picard–Fuchs operator, so the
   sub-VHS generated by the holomorphic 2-form has **rank exactly 3**.
2. **[A, cited]** the K3s are **projective** — A–vS build them as complete intersections in
   G(2,6) and ℙ³×ℙ³ (E-009).
3. **[B, two sources, both read]** `T(X)⊗ℚ` is an **irreducible** Hodge structure — Zarhin 1983
   Thm 1.6(a) p.207, and Huybrechts Lemma 3.2.7 + 3.3.1. Zarhin's own p.200 also supplies
   `dim V(Y) = b₂ − ρ`.
4. A nonzero sub-Hodge-structure of an irreducible one is the whole thing ⇒ **T = 3** ⇒
   **ρ = 22 − 3 = 19**.

Note this did **not** come from Stienstra–Beukers 1985. That paper is **off the critical path**
and remains unfetched — stop treating it as a blocker.

## Gate E consequence

Criterion 1 moves from **UNRESOLVED** to **scoreable**. If it passes, Gate E can reach
**6/6** and the CONDITIONAL ceiling set by T0 decision D1 lifts. That is your call to make on
the evidence, not mine — I am supplying the prior, not the verdict.

**Still true and unchanged:** there is no valid D-3 empirical run in the repo. The only one was
E-010's fabricated batch, now retracted. If you have a real run, it remains the single biggest
gap in the project.

**Generated-by:** Opus 5 (Stream 2) | **Verified-by:** `data/certificates/C2_cooper_s{7,10}_v3.json`, `ESCALATIONS.md` E-011, `refs/literature_provenance.txt` | **Reviewed-by:** Xavier (T0) — decision authority delegated 2026-07-26
