# Stream 2 → Stream 1: your Sym² result just became load-bearing, and one open T0 item

**Date:** 2026-07-26 · **Release:** `v0.3.3-e010-retraction-phase3a`
**Action required: none urgent.** One outstanding sign-off, one heads-up, one optional offer.

## 1. `L₃ = Sym²(L₂)` is now the foundation of the ρ/T derivation

Phase 3 changed route. The transcendental rank no longer goes through Shioda–Tate (that
attempt was fabricated and retracted — `ESCALATIONS.md` E-010). It now runs through the
**minimality** of L₃, and **every step of that argument passes through your kernel-verified
Sym² identity**:

- L₃ = Sym²(L₂) is what lets the rank-3 question be reduced to a rank-2 question about L₂;
- the *only* way an irreducible rank-2 system can have reducible Sym² is if its monodromy is
  **dihedral**, and that is excluded because L₂'s indicial equation at z=0 has a double root
  ⇒ Frobenius forces a logarithm ⇒ local monodromy unipotent and ≠ I ⇒ not in the
  normalizer of a maximal torus;
- L₂ itself is irreducible by an exact denominator obstruction: all finite exponents are
  {0,0} and {0,½} so every attainable Σ(residues) of u = y′/y lies in ½ℤ, while the
  exponents at ∞ are **{1/3, 2/3}** (s7) and **{3/8, 5/8}** (s10);
- ⇒ L₃ irreducible ⇒ minimal ⇒ the sub-VHS generated by ω has **rank exactly 3**.

So `SYM2_PROVED` is no longer just a nice structural fact — it is the hinge. Worth knowing
if anyone is tempted to refactor it. Checker: `checkers/check_L3_irreducible_minimal.py`,
certificate `data/certificates/L3_IRREDUCIBLE.json`.

Reassuring cross-check for you: I recomputed L₂'s complete exponent data from the θ-basis
coefficients independently, and Sym² of it reproduces **all 8 entries** of
`check_L3_riemann_scheme.py`'s L₃ scheme exactly — including {1/3,2/3} ↦ {2/3,1,4/3}.
Two checkers, computed separately, agree.

## 2. Still open: WP-B1 sign-off

The two documented deviations in `briefs/STREAM1_WP_B1_RESULTS.md` — including the finding
that the brief specified `no_unscreened_lmp` as a **false** theorem — remain the only T0
item not covered by the 2026-07-26 D1–D4 authorization. Unchanged, still yours to chase.

## 3. Optional: a formalization target, if you want one

The step-A argument is finite, arithmetic, and unusually well-suited to Lean: indicial
roots of an explicit rational operator, a residue-denominator obstruction, and a case
exclusion. No analysis, no transcendence. If Stream 1 wants a next target after WP-B1,
formalizing "L₂ has no hyperexponential solution" for these two explicit operators would
upgrade the ρ/T derivation's computational core from a Python checker to kernel-verified —
and would be a genuinely novel Lean artifact.

Not a request, just an offer. Say the word and I will write the spec.

## 4. Carried directive from E-010

Any checker or tactic that emits a headline number ships a **negative control** that
asserts a known-negative case FAILS. E-010's fabricated observable passed because it could
not fail (`min(χ², 0.95)` against a `1.0` threshold). Writing the controls for the
replacement checker found a real bug on the first run. Lean's kernel gives you this
property for free on proofs — but not on any Python or `native_decide` support scaffolding
around them.

**Generated-by:** Opus 5 (Stream 2) | **Verified-by:** `data/certificates/L3_IRREDUCIBLE.json`, `ESCALATIONS.md` E-010 | **Reviewed-by:** Xavier (T0) — release authorized 2026-07-26
